# WebSocket tutorials (Read the readme in each of the folder before reading the PDFs)
* WebSocket1 -- basic concepts (ignore the example)
* WebSocket2 -- simple implementations
* WebSocket3 -- implementation with autowiring